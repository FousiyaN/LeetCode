class Solution(object):
    def reverseString(self, s):
        first = 0
        last = len(s)-1
        while(first<last):
            swap=s[first]
            s[first]=s[last]
            s[last]=swap
            first+=1
            last-=1
        return s
