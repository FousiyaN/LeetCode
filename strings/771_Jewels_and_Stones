class Solution(object):
    def numJewelsInStones(self, jewels, stones):
        count = 0
        stone = stones.split()
        for stone in stones:
            if stone in jewels:
                count+=1
        return count
